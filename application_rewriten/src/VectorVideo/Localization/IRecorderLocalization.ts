
module Localization {
	
	/**
	 * A set of strings for localizating the recoreder UI
	 */
	export interface IRecorderLocalization {				
		UploadWasSuccessful: string,
		RedirectPrompt: string,
		FailureApology: string,		
		NoJS: string,
		WaitingText: string
		
		// buttons
		RecPause: string,
		Record: string,
		Pause: string,
		Upload: string,
		ChangeColor: string,
		ChangeSize: string,
		Erase: string,
		EraseAll: string
		
	}
	
}