/// <reference path="../../typings/tsd.d.ts" />

import { expect } from 'chai';

describe("Test the behavior of the Vector Screencast SVG parser", () => {
	it("must parse correctly formed data", () => {
		//throw new Error("Not implemented yet");
		expect(5).to.equal(5);
	});
});